# LeadConnector (GoHighLevel) inbound webhook that receives registrations.
# Optional — src/app/api/register/route.ts falls back to the production hook.
# Set this to point a preview/staging deploy at a different workflow.
LEAD_WEBHOOK_URL=
